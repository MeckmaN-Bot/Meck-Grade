fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9
opencv-python>=4.10.0.84
Pillow>=11.2.1
numpy>=2.0.0
scikit-image>=0.24.0
scipy>=1.14.0
pydantic>=2.7.1
aiofiles>=23.2.1

# Card lookup + PDF export (auto-installed; all gracefully degrade if missing)
requests>=2.31.0
reportlab>=4.1.0

# Optional: OCR for card name extraction (install separately if desired)
# pytesseract>=0.3.10
# Also requires Tesseract binary: https://tesseract-ocr.github.io/tessdoc/Installation.html
